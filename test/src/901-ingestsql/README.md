This test is intended to be possible to run in the following ways:
- through /test/run.sh testing framework (apparently requires systemd);
- or standalone via ./run_test;
- either with or without systemd.
